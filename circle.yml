machine:
  python:
    version: 3.5.2


dependencies:
  pre:
    - pip install --upgrade pip virtualenv

  override:
    - pip install -r requirements.txt
    - pip install coveralls

  post:
    - coveralls
